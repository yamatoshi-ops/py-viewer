  #0.2 と #1 の機能差分                                                                                               
                                                                                                                      
  #0.2 が持っている機能（継続）                                                                                       

  ┌─────┬──────────────────────────────────────────────────────────────┐
  │  #  │                             機能                             │
  ├─────┼──────────────────────────────────────────────────────────────┤
  │ 1   │ 行レイアウト [ch名/値] [波形グラフ]                          │
  ├─────┼──────────────────────────────────────────────────────────────┤
  │ 2   │ Parquetデータ読み込み（パスがハードコード）                  │
  ├─────┼──────────────────────────────────────────────────────────────┤
  │ 3   │ チャンネル自動検出・複数ch表示                               │
  ├─────┼──────────────────────────────────────────────────────────────┤
  │ 4   │ 全グラフ ズーム・パン同期                                    │
  ├─────┼──────────────────────────────────────────────────────────────┤
  │ 5   │ ホバー時の全ch値同時表示（左列）+ 全グラフ縦断スパイクライン │
  ├─────┼──────────────────────────────────────────────────────────────┤
  │ 6   │ スクロールズーム・ツールバー（ホバー表示）                   │
  └─────┴──────────────────────────────────────────────────────────────┘

  ---
  #1 で追加・改善するもの

  🆕 新規機能（#0.2 には存在しない）

  ┌─────┬─────────────────────────────────────────────────────┬────────────┐
  │  #  │                        機能                         │    区分    │
  ├─────┼─────────────────────────────────────────────────────┼────────────┤
  │ A   │ ファイル選択UI（任意のParquetを読み込める）         │ データ入力 │
  ├─────┼─────────────────────────────────────────────────────┼────────────┤
  │ B   │ データ前処理ユーティリティ（CSV/MAT → Parquet変換） │ データ入力 │
  ├─────┼─────────────────────────────────────────────────────┼────────────┤
  │ C   │ チャンネル表示/非表示切り替え                       │ UI         │
  ├─────┼─────────────────────────────────────────────────────┼────────────┤
  │ D   │ カーソル差分計算（Δt / ΔV / ΔA）                    │ 解析       │
  └─────┴─────────────────────────────────────────────────────┴────────────┘

  🔧 #0.2 の既知課題（設計書 §4 改善点より）

  ┌─────┬───────────────────────────────────────────┬──────────────────────────────────────────┐
  │  #  │                   課題                    │                 対応方針                 │
  ├─────┼───────────────────────────────────────────┼──────────────────────────────────────────┤
  │ E   │ Y軸に単位が未表示（ch名のみ）             │ メタデータJSON or カラム名規則で単位付与 │
  ├─────┼───────────────────────────────────────────┼──────────────────────────────────────────┤
  │ F   │ マウスアウト時にホバー値が --- に戻らない │ mouseleave イベントで Store をリセット   │
  └─────┴───────────────────────────────────────────┴──────────────────────────────────────────┘

  ▎ G「ホバー値をグラフ内表示」 と H「カーソル差分のUI設計（マーカー方式 or レンジセレクタ）」 
  は企画書の「未決定事項」のため、#1 設計書で決める必要あり。

  ---
  まとめ：#0.2 → #1 の変更イメージ

  #0.2（完成）                     #1（追加・改善）
  ─────────────────────────────────────────────────
  ハードコードパス           →  ファイル選択UI (A)
  （前処理なし）             →  CSV/MAT→Parquet変換 utils/ (B)
  全ch常時表示               →  ch 表示/非表示トグル (C)
  （差分計算なし）           →  Δt/ΔV/ΔA カーソル差分 (D)
  単位なし Y軸               →  単位付き Y軸 (E)
  マウスアウトで値残る       →  マウスアウトで --- リセット (F)

  ---
  次のアクション候補:
  - 設計書を作る → proto_1_設計書.md に上記をベースに機能設計を落とし込む
  - 実装から入る → A（ファイル選択UI）が最優先（ハードコード除去）なのでそこから着手
